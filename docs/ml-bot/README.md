# ML / Self-Play Bot Initiative

> **Status:** Active — **▶ 20M→40M CONQUEROR EXTENSION IN FLIGHT (launched 2026-07-09, [D-35] pre-registered):** `ppo-v3-scratch` resumed on shodan at its pinned `464a2ee` in a copied run dir (`ppo-v3-scratch-40m`; resume verified in both halves at step 20,004,864; Task-Scheduler-owned; ~30 h at the observed 186 fps) toward an absolute 40M budget — the [D-31] curve ended rising (final checkpoint = peak, +33.9 [32.2, 35.5] vs Lookahead), so the open question is whether budget was the binding constraint. Bars pre-registered BEFORE any extension result existed ([D-35]): resume-health tripwire at the first eval point (must stay BEAT vs Lookahead), early-stop on three consecutive points with CI-upper < 32.2, **primary/ship = BEAT the shipped Conqueror head-to-head (`ppo:gate --bar Conqueror`, CI > 0)** + retain the Lookahead floor + a behavioral co-read (§10.4 clock-hack / [D-30] basin panels, flag→escalate) — **no-ship default** (Conqueror keeps the 20M weights unless every bar clears; Blitz/Survivor unchanged either way). The [D-29] curve is graded live on the mini (fresh same-knob 20×150 walk in watch mode over a base64-mirrored eval dir — RUNBOOK §9). **#157 ladder honesty: field RATIFIED as option (a)** (the player-visible roster — Conqueror, Blitz, Survivor, Lookahead, Strategist, Expectimax); the once-only fresh-seed matrix is **DEFERRED until this run's ship/no-ship** so it measures the roster that actually ships. Decision: [D-35]; session: LOG.md. — Prior status: **✅ WAVE-2 GRADED → PREDATOR CLOSED under the current wire (2026-07-08, [D-33]):** both 1M Predator pilots (`ppo-v3-pred-b15`/`-b25`, placement + elim-bounty {0.15, 0.25} off the v3 base) ran clean on shodan and were graded on the full pre-registered battery in the §10.3 calibration field — and they fail in **complementary corners**: **b15** BEAT the Lookahead floor +24.7 [20.2, 29.2] with every tripwire clear (the _anti_-turtle) and separates from Survivor on turnsToWin + avgPlacement, but its kill-edge over v2 Survivor (Δ+0.16 [0.00, 0.31]) falls short of the +0.26 confirmatory bar; **b25** clears the kills bar (Δ+0.39 [0.17, 0.61]) but is KILLED by the [D-30] turtle-basin tripwire on `zeroAttackTurnFrac` (Δ+0.178 > 2× the bar @0.5M, still over @1M). No bounty in the sweep does both — though the **v2→v3 dose-response sign flip** (kills now RISE with bounty; they fell in v2) shows `eliminatesDefender` fixed representability's direction, leaving credit _price_ as the binding failure. Per §10.8 + Ivan's standing ruling: **revisitable only with a frame-level kill-attribution fix** (a [D-28]-pattern header/timing change — NOT the #85/#134 start-eliminated-seat guard, which changes nothing on this wire); no coef re-sweeps. The shipped trio (Conqueror + Blitz + Survivor v2) stands unchanged; the §10 persona slate is now fully resolved (Wave 3 never fires). Numbers: RESULTS.md 2026-07-08 §10.2; decision: [D-33]; session: LOG.md. — Prior status: **✅ WAVE-2 PREP COMPLETE (2026-07-08):** all three Mac-local gates for the Predator pilots (PERSONAS §10.3, 2×1M) are done — the scavenge co-read + tripwire panel landed and **RATIFIED 0.31 / 5.64 / 0.91** with the §10.3 kills bar recomputed off v2 Survivor at **+0.26** (2026-07-06, PR #127), and the final item landed 2026-07-08: the **[D-30] absolute winPct floor RATIFIED at 35 unchanged** and the **§10.4 clock-hack panel RATIFIED at 0.05 / 0.31 / 0.18** (window 50), calibrated per the pre-registration off the Wave-1 control's 0.5M/1M eval checkpoints vs the Conqueror base in the calibration field (the control runs ~50% there — the v2-era "control at 34.5" scare was a field artifact; the spike axis is unmeasurable on win-mode arms, n=2, so the rule gained an explicit <5-live-runs no-power exclusion). **Next: launch Wave 2** — the two 1M Predator pilot arms on shodan (Ivan-gated, durable supervisor per RUNBOOK §8a/§8e), running `behavior:preflight` + `ppo:arm-throughput` at launch time, then grade in the calibration field against the ratified panels + the +0.26 kills bar + the Lookahead floor + v2-Survivor separation. Numbers: RESULTS.md 2026-07-08; session: LOG.md. — Prior status: **✅ WAVE-1 PERSONAS GRADED (2026-07-06, [D-32]):** the 3-arm v3 retrain (`ppo-v3-{conq-ctl,blitz,survivor}`, 3×3M off the Conqueror base) ran clean overnight and was graded on the full §10 battery — **Blitz-v3 SHIPPED** (PR #120: BEAT v2 Blitz **+11.6 [8.7, 14.4]**, TIE base, BEAT Lookahead +29.6; the §10.5 dual-control signature flag was accepted as an **attribution caveat** by explicit maintainer override — the style contrast vs Conqueror is real but mostly continuation drift, not the γ0.99 lever); **Survivor-v3 KILLED** (BEHIND v2 −5.9, BEHIND base −20.8, signature fails both comparators → keep v2; the §10.6 placement-as-flagship scratch trigger is dead); the control TIED the base head-to-head (+1.1 — the §10.8 flag dissolved as a field artifact); §10.4 clock-hack panel all clear. **Next: Wave 2 — the Predator pilots (§10.3, 2×1M)**, gated on three Mac-local prep items: build the **scavenge co-read** (a pre-committed §10.8 kill condition, not yet implemented), recompute the §10.3 kills bar off **v2 Survivor** (the comparator fallback fired), and recalibrate the tripwire winPct floor / ratify the §10.4 thresholds from the control's 0.5M/1M probes. Numbers: RESULTS.md 2026-07-06; session: LOG.md; decision: [D-32]. — Prior status: **✅ ENCODING-V3 BARS ALL PASSED (2026-07-05, [D-31] §4):** `ppo-v3-scratch` (20M from scratch on the fixed v3 encoder, pinned `464a2ee`) finished **clean 2026-07-04** (exit 0, attempt #1, zero restarts) and passed **every pre-registered bar**, gated locally off the fixtured eval stream — **primary** (the clean encoding A/B) BEAT `ppo-scratch-long` head-to-head **+6.1 pp [3.9, 8.4]** fresh-seed-confirmed; **ship** BEAT Survivor (the strongest previously-shipped net) **+5.5 pp [3.8, 7.2]**; floor vs Lookahead **+33.9 pp [32.2, 35.5]**, with the strength curve rising monotonically into the final checkpoint (no tail regression). The v3 net is now the strongest net in the repo. **SHIPPED as Conqueror (2026-07-05, [D-31] §5):** packed export of `ppo-v3-scratch/ppo.pt` → `src/ai/conquerorPolicyWeights.js` (verified bit-identical to the gated `eval-020004864` checkpoint, JS↔Python parity fixture in-suite), Conqueror rewired off the `ppo-long` alias onto its own file; hidden `ai_ppo` keeps the frozen v2 `ppo-long` weights as the `ppo:gate` bar. **Next (PERSONAS §10.7):** the Wave-0 eval builds — the **[D-29] scorer landed 2026-07-05** (`npm run ppo:curve` + the `runGateSweep` extraction; `STRENGTH_CURVE.md` "As built"), the §10.7 item-4 head-to-head bar loader landed 2026-07-05, the **item-2 Holm adjustment landed 2026-07-05** (`holmSignatures` + `--holm-family` in `behavior:profile`; `EVAL_HARNESS` §3.3 "As built"), the **item-3 profile-pairing separation script landed 2026-07-05** (`npm run behavior:separation` over `behavior:profile --json` reports, incl. the §10.3 relative kills bar and `--require-separated`; `EVAL_HARNESS` §3.5 "As built"), and the **item-5 launch pre-flight + negative controls 1–2 landed 2026-07-05** (`npm run behavior:preflight` — the #97 probe-path load check + the A/A `signatureNoiseFloor` (CI-equivalence, halts only on a BIASED axis) + NC2's `ppo:curve --test-retest` reader; `EVAL_HARNESS` §3.9 "As built"), and the **item-6 3-arm throughput probe landed 2026-07-05** (`npm run ppo:arm-throughput` — two timed passes, 1 arm vs 3 concurrent, reporting the per-arm contention penalty + a one-sided go/no-go on `N_ENVS`; measures the env-sim ceiling so RED is conclusive; run on shodan, RUNBOOK §8e). **All six Wave-0 items are now built → the next step is Wave-1 persona retrains** (Conqueror-control + Blitz-v3 + Survivor-v3, 3×3M concurrent) warm-started from `ml/runs/ppo-v3-scratch/ppo.pt` — Ivan-gated shodan launch. Numbers in `RESULTS.md` (2026-07-05 section); session detail in `LOG.md`. — Prior in-flight status **(2026-07-02, [D-31]; clock redefined 2026-07-03):** the observation encoding is now v3 (append-only: owner-attribute + income-consequence node/edge features, turn order, clock; `SUPPORTED_ENCODING_VERSIONS = [2, 3]` keeps every shipped v2 net running via slice-compat). **2026-07-03:** a five-agent review of the v3 harness PRs (#101–#105) found the board clock encoding the wrong unit (rounds, not player-turns — it never saturated at truncation); fixed in place via a new engine `turnsTaken` counter, the column renamed `turnClockNorm` (see the [D-31] amendment — sound because no v3-trained weights exist). The in-flight broken-clock `ppo-v3-scratch` run was discarded (archived at ~6.6M/20M) and **relaunched from scratch on the fixed encoder 2026-07-03** (pinned `464a2ee`; first 100k checkpoint stamped v3). Bars unchanged: primary = beat the v2 scratch control head-to-head, ship bar = beat Survivor (then it ships as Conqueror; persona re-batch incl. a Predator revival is a separate wave — **now designed + pre-registered in [PERSONAS.md §10](./PERSONAS.md) (2026-07-04)**, with Predator's kill-gate scoped "closed under the current wire"). — **🏁 PHASE 3 BEAT ACHIEVED (2026-06-29): the 20M PPO self-play run beats `ai_lookahead` by paired Δ win% +27.7 [25.0, 30.4] (PPO 40.4% vs 12.7%), corroborated by a from-scratch control (+26.9) — the D-7 headline gate is MET. The winning weights are merged into `ppoPolicyWeights.js` (Phase 4 shipped). **Superseded by [D-27] (2026-06-30):** the player-facing roster is now the self-play personas — Conqueror ships these `ppo-long` weights under a friendly name, while `ai_ppo`/`ai_bc` are hidden from players (kept as the dev-harness `ppo:gate` baseline).** Earlier history below. — `ai_expectimax` is at **parity with `ai_lookahead`** (D-9: win% dead-heat, significantly out-places it, ELO co-leader, 1v1 tie). The eval-rework spike (Phase 0.5) was **killed at 2/4 swings** — no structural eval term beat the baseline ([D-11]) — confirming Track-A search is tapped out at parity. The Phase 0 headline gate (a \_significant win% edge\* over Lookahead) stays **open**. Track B (learned policy) — Phase 1 (self-play harness hardening) is ✅ DONE ([D-12]). **Now: Phase 2 — imitation baseline (clone `ai_lookahead` to a small in-browser ONNX net) is 🟨 IN PROGRESS. Kicked off 2026-06-23: D-Encoding finalized (Proposed → Accepted, code-grounded + information-completeness verified against the teacher), a tracer teacher shard generated (2000 clean games, round-trips, teacher labels + win-prob edge feature validated), and duplicate-seat support added to `selfplay.mjs`. Corpus field validated ([D-15]): the full 7-bot arena field (imitate Lookahead's seat) — 85% decisive, 55%-attack balanced labels; a pure `N×Lookahead` mirror was a turtle equilibrium. Tensor-expansion pass landed: `src/arena/encodeObservation.js` (pure D-Encoding encoder) + `npm run encode-corpus` (lean corpus → NumPy-loadable packed tensors; CSR edges). Mask-matches-`getValidMoves` and binary round-trip both regression-tested (885 green). BC trainer now scaffolded in-repo at `ml/` ([D-16]): a runnable PyTorch package (masked per-edge MLP + aux value head, segmented CE, game-level split) reading the packed tensors → ONNX (logits-only single-step graph, ORT parity ≈5e-7). Verified on the 300-game sample (val move-match 33%→47% in 8 untuned CPU epochs; hermetic pytest suite). Next: the parity run on the GPU box (100k–1M-game corpus + tuning) and the in-browser ONNX-Runtime-Web bot slice.** **Update 2026-06-24:** the 100k parity run + STOP-de-bias retrain are done. The masked per-edge MLP clones Lookahead's _behavior_ but not its win rate — best **6.4% vs Lookahead's ~20%** (the merged "0% win / rank-3 ELO 1275" row was a broken registration measuring a do-nothing bot, fixed in #52). The clone's STOP over-prediction was cured by a weighted-CE retrain + STOP-calibration checkpoint selection (`--select-by stop-cal`): realized arena STOP **71%→49%**, native win **3.6%→6.4%**, shipped to `src/ai/bcPolicyWeights.js`. The residual ~13 pt gap is the encoding/arch ceiling, not calibration — **pure BC is at its ceiling and literal Phase-2 parity won't come from it; it carries to Phase 3 (GNN / PPO), warm-started from this calibrated clone.** **Update 2026-06-25 (Phase-3 ceiling probe complete):** **(1) Capacity ruled out** ([D-17]): 10× params (102k→1M) → flat-to-declining win% (6.7→5.1%). **(2) Encoding-v2 → FEATURE-LIMITED** ([D-18]): six engineered local-neighbourhood features (node 5→8, edge 4→7, `ENCODING_VERSION 2`, no raw adjacency blob) lifted val move-match **0.57→0.73** _and_ — the decisive part — arena win% **6.7%→12.5%** (CIs disjoint), ~halving the native gap to Lookahead (now ~4.6 pt). v2 weights shipped to `bcPolicyWeights.js`; **no STOP bias needed** (native peak at `stopBias 0` — the richer features cured the turtle). The per-edge MLP was feature-starved, not factorization-saturated. BC's residual is the **imitation ceiling** → **forking to PPO**, with the v2 observation as the policy input (the durable, reusable artifact). **Update 2026-06-25 (Phase 3 — PPO kicked off):** architecture finalized via a scope-grounded surface-map + adversarial verification ([D-19], the way [D-12] grounded Phase 1). Design: **PettingZoo AEC** with one learner seat external + 7 seats in-process; a **persistent Node env-server over a local binary socket** (not per-step JSON); the policy **reuses the `EdgePolicyNet` trunk + per-edge head** with a fresh scalar critic; the **observation IS the v2 encoding**. **Four decisions (Ivan):** warm-start from v2-BC + a short from-scratch control; **full 8-FFA vs a heterogeneous PFSP league from step one** (deviating from the old 3–4p-symmetric plan — [D-15] mirrors turtle); sparse terminal-win reward first (shaping only if too slow; placement = the ELO trap); fixed env-step budget + kill threshold. **Verification corrected two claims + found one gate-breaker:** the throughput bottleneck is the **in-process heuristic opponents, not the wire** (~1.7–5 ms/learner-step; reachability unproven until a probe runs); the variable-length edge head needs **pad-to-`MAX_EDGES` + mask** (MaskablePPO wants fixed Discrete); and an **SB3→`EdgePolicyNet` repack adapter** is required + must be parity-asserted or the graded bot ≠ the trained policy. **Now: build the first tracer slice** (Node env-server → cross-bridge action-encoding parity test → throughput probe — the decision-independent, highest-de-risk steps first).
>
> **Last updated:** 2026-07-09
>
> **Owners:** Ivan (+ Claude)

A tracked, multi-session effort to produce a DiceWars bot whose play is shaped by
machine learning / self-play. This folder is the persistent home for the plan,
decisions, running log, and empirical results so progress survives across days
and Claude Code sessions.

---

## Goal

Produce a DiceWars bot that:

1. Runs **in-browser as just another bot** — same contract as every other bot:
   `(BotState) → { from, to } | null`.
2. Is **measurably stronger than `ai_lookahead`** (the current strongest bot —
   re-baselined from `ai_strategist` per [D-7](./DECISIONS.md); Strategist had been
   assumed strongest but Lookahead beats it decisively), judged by the gate below.

## Verdict (from the 2026-06-21 feasibility analysis)

**Large-but-doable — not too big.** The engine is an excellent RL environment:
pure, immutable, seedable, ~150 games/s/core headless, with a free legal-action
mask (`getValidMoves`) and an exact dice-odds table (`WIN_TABLE`). The plumbing is
mostly already in place.

The **hard, uncertain** part is not the plumbing — it's that from-scratch
self-play RL has a real chance of **never beating the strong heuristic bar** (the
2026-06-21 analysis said `ai_strategist`; it is now `ai_lookahead`, which is even
stronger — see [D-7](./DECISIONS.md)). The most
relevant prior art (a KTH Risk AlphaZero/ExIt thesis; Moy & Shekh's hex-grid
wargaming) shows naive self-play nets _losing_ to good heuristics until they're
bootstrapped with heuristic/supervised data. `ai_strategist` is a strong,
exact-odds baseline — not a strawman.

**Strategy: search first, learning second.** Bank a likely win with a pure-JS
search bot before spending GPU-weeks on RL, and gate every step on the same
empirical bar.

## The bar — evaluation gate (used at EVERY phase)

A candidate is only "better" if it beats **`ai_lookahead`** — the current
field-strongest bot, pinned at `596f781` (re-baselined from `ai_strategist` per
[D-7](./DECISIONS.md)) — on **`npm run arena:sweep`** (multi-seed, mean win%/ELO
with 95% confidence intervals), controlling for seat/turn-order so we measure skill
not first-mover luck. The edge must be **statistically significant**, not a
single-seed fluke. `ai_strategist` is kept as a secondary reference. Every run gets
a row in [`RESULTS.md`](./RESULTS.md).

---

## Two-track strategy

- **Track A — pure-JS search (no ML).** Chance-node expectimax reusing
  `ai_strategist`'s evaluation as the leaf heuristic and `WIN_TABLE` for exact
  chance-node expectation. Days of work, likely beats the baseline, and validates
  the arena as the evaluation gate. (Phases 0–1 also serve Track B.)
- **Track B — learned bot.** Keep the JS engine as the single source of truth;
  bridge to Python for **training only** (PettingZoo + MaskablePPO self-play),
  export the small policy net to ONNX, and run it in-browser via ONNX Runtime
  Web. (Phases 2–4.)

---

## Status dashboard

| Phase | Title                                 | Status                      | Go/No-Go gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----: | ------------------------------------- | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     0 | Quick-win search bot                  | 🟨 Shipped; gate open (tie) | **Press-mechanism landed (D-9):** posture-adaptive threshold + elimination term + low-odds risk floor. `ai_expectimax` now **ties `ai_lookahead`** (16,800 seat-fair games: win% 22–23% vs 22.5–23.9%; **significantly out-places it**, paired 51.2%, p≈0.002; ELO co-leader; 1v1 tie). Beats Strategist (13–14%). Rank 6/7 → **joint-strongest**. Headline gate (sig. **win%** edge over Lookahead) still **open** — it's a tie. Eval rework (Phase 0.5) tried & killed ([D-11]). **Next: Track B.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
|   0.5 | Eval-rework spike (Track A)           | ❌ Killed 2/4 swings        | **No structural eval term beat the D-9 baseline** ([D-11]): `mergePotential` / `fieldRivalIncome` / `trappedDice` all neutral-at-best, harmful when grown, across 2 seeds × 2 power levels. Same parity ceiling as D-8/D-9 → Track-A search tapped out; pivot to Track B.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
|     1 | Harness hardening for self-play       | ✅ Done (2026-06-23)        | **Gate met.** Reproducible + instrumented + parallel committed harness (`scripts/selfplay.mjs`), and the per-move alloc trims landed — **≈1.9× pure-engine throughput** (drop the redundant per-end-turn `areas` double-clone + gate `findLargestConnectedGroup` to the 0–2 players an action changes), byte-identical games, near-linear scaling (1→4 workers 3.04×). Numbers in `RESULTS.md` ([D-12]). **Next: Phase 2.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
|     2 | Imitation baseline (de-risk learning) | 🟨 BC at ceiling            | Can a net clone the strongest heuristic (`ai_lookahead`) to ~parity, in-browser via ONNX? **2026-06-23:** D-Encoding finalized (Accepted); tracer shard (2000 clean games, round-trips); duplicate-seat support added to `selfplay.mjs`. **Corpus field validated ([D-15], RESULTS):** the **full 7-bot arena field** (imitate Lookahead's seat) — 85% decisive, 55%-attack balanced labels, ~81 teacher steps/game; a pure mirror was a turtle equilibrium. **Tensor-expansion pass landed:** `encodeObservation.js` + `npm run encode-corpus` (lean → packed NumPy tensors, CSR edges); mask-match + binary round-trip tested. **BC trainer scaffolded ([D-16]):** in-repo `ml/` PyTorch package (masked per-edge MLP, segmented CE, game-level split) → ONNX (logits-only, ORT parity ≈5e-7); verified on the 300-game sample (val move-match 33%→47%, 8 epochs; hermetic test suite). **2026-06-24 — done & at ceiling:** the 100k clone matches Lookahead's _behavior_, not its win% (best **6.4% vs ~20%**; the merged "0% / ELO-1275" row was a broken registration → do-nothing bot, fixed #52). The clone's STOP over-prediction (71% realized) was cured by a **weighted-CE retrain + `--select-by stop-cal`** checkpoint selection → realized arena STOP **49%**, native win **3.6%→6.4%**, shipped to `src/ai/bcPolicyWeights.js`. Residual ~13 pt = encoding/arch ceiling ([D-Encoding]) → **pure BC is tapped out; literal parity carries to Phase 3 (GNN / PPO) as the calibrated warm-start.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
|     3 | Self-play RL (PPO)                    | ✅ BEAT (2026-06-29)        | Does PPO beat `ai_lookahead` (per D-7) with a significant **win%** edge? **Tracer slice closed (#61, 2026-06-26):** `repack→export→register→gate` proven on a real trained PPO policy (11.5% vs Lookahead 15.1%, paired Δ −3.6 pp, ❌ BEHIND — the loop-closer expectation). **Scaling design fixed ([D-22]):** the PFSP league is **Node-resident** (pool+sampler+win-rate book in the env-server; Python only produces snapshot artifacts — forced by the i32-only wire); build **one** league pipeline where **fixed-field = its empty-pool mode**, and run a cheap fixed-field "does PPO learn past BC?" gate **first** before flipping PFSP on (reconciled with [D-19]). **Task A PASSED (PR #63, Δ+33.4); Task B PFSP league B0–B6 DONE 2026-06-27 — feature-complete.** The B5 re-probe ([D-24]) shows env-sim is NOT the bottleneck (GREEN at every R, ~25–45× the bar), **R=3 locked**, the snapshot field doesn't turtle (global ≥90%); the binding rate is the SB3 learner loop. **B6 ([D-25], PR #69 `d637a06`)** added league persistence (`toJSON`/`restore` + a pluggable `SharedDiskStore`, live-gated until task E) as a standalone Node PR — opt-in, byte-identical to B5 when off. **Task C/E done → the long BEAT run is COMPLETE (2026-06-29):** a from-scratch control (1M, `+26.9 [23.8, 30.0]`) resolved the task-A fixed-field-exploitation caveat, then the **20M BC-warm-start run gated `+27.7 [25.0, 30.4]` → ✅ BEAT** (PPO 40.4% vs Lookahead 12.7%, clean run, attempt #1, 0 restarts, R=3 PFSP league). The D-7 headline win% gate is MET at full budget against a held-out-general league. Winning weights merged into `ppoPolicyWeights.js` → ship via Phase 4. **Encoding-v3 re-run ([D-31]) — ALL BARS PASS (2026-07-05):** primary **+6.1** h2h vs the v2 scratch control (the clean encoding A/B), ship **+5.5** h2h vs Survivor, floor **+33.9** vs Lookahead, fresh-seed-confirmed → the v3 net is the new strongest; ships as **Conqueror** per [D-31] §5. |
|     4 | Ship the strongest bot                | ✅ Done (2026-06-30)        | Winner wired as in-browser bot + in tournament pool. **`ai_ppo` is already wired (PR #74) and decoupled from its weights;** this PR swaps in the BEAT-gated 20M weights (`ppoPolicyWeights.js`, `f6be9b91…` replacing task-A `1a754eef…`) with no code change → the in-game AI picker (difficulty 5) + the daily tournament/leaderboard picked up the BEAT-gated net. **Superseded by [D-27] (2026-06-30):** the player-facing roster is now the self-play **personas** (Conqueror/Blitz/Survivor); Conqueror ships these exact `ppo-long` weights under a friendly name, while `ai_ppo`/`ai_bc` are hidden from players and kept as the dev-harness `ppo:gate` baseline. See the persona-roster LOG/RESULTS/DECISIONS entries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

**Legend:** ⬜ Not started · 🟨 In progress · ✅ Done · ⛔ Blocked · ❌ Killed (gate failed)

> Update this table whenever a phase changes state. Full phase detail —
> tasks, acceptance criteria, kill-gates — lives in [`PLAN.md`](./PLAN.md).

---

## How to use this folder (future sessions — read this first)

- **[`PLAN.md`](./PLAN.md)** — the phases in detail: tasks (checkboxes),
  acceptance criteria, and the go/no-go gate per phase. The source of truth for
  "what's next."
- **[`LOG.md`](./LOG.md)** — append-only session journal. **At the end of each
  working session, add an entry** (what changed, what we learned, dead ends).
  This is how context survives across days and sessions.
- **[`RESULTS.md`](./RESULTS.md)** — the empirical scoreboard. Every
  `arena:sweep` run gets a row. This is how we decide "better."
- **[`DECISIONS.md`](./DECISIONS.md)** — why we chose what we chose (PPO over
  AlphaZero, JS-engine-as-source-of-truth, etc.). Add an entry at each real
  fork-in-the-road.
- **[`PERSONAS.md`](./PERSONAS.md)** — the **reward-persona roster** plan (the
  post-BEAT initiative): train several PPO bots with distinct _play-styles_
  (Conqueror/Blitz/Expansionist/Predator/Survivor) rather than one max-strength net.
- **[`EVAL_HARNESS.md`](./EVAL_HARNESS.md)** — the **behavioral-eval harness** spec
  (the measurement half of the roster): profiles a bot on behavioral axes vs a
  control and gates a pre-registered persona signature PASS/FAIL — the complement to
  `ppo:gate`'s "is it stronger?". **Phase 2a (persona gating) is BUILT + MERGED
  (PR #80, "bite E1"):** `npm run behavior:profile` can load a `Name=weights.js`
  export and emit its PASS/FAIL signature. The persona _weight files_ are the only
  thing still pending.
- **[`STRENGTH_CURVE.md`](./STRENGTH_CURVE.md)** — the **checkpoint strength-curve
  harness**: grade a PPO run's per-checkpoint stream (dropped every 1M steps) to build
  a strength-vs-steps curve and catch regressions / plateaus that only-grading-the-final
  checkpoint misses (motivated by the 20M→23M `ppo-conqueror` regression). **Phase 0
  (producer) SHIPPED (PR #97); Phase 1 (the `ppo-strength-curve.mjs` scorer) design
  REVIEWED 2026-07-01 ([D-29]) — amendments applied, all five open questions resolved
  (watch mode, in-field references, run-paired k=2 regression detector, mandatory
  fresh-seed confirmation before shipping); ready to build.**

---

## Key grounded facts (so future sessions don't re-derive them)

Verified against the code during the 2026-06-21 analysis:

- **Bot contract:** `(BotState) → { from, to } | null`; `null` ends the turn.
  Deploy via `adaptModernBot` + an entry in `src/arena/builtInBots.js` (or a
  community-bot registry entry).
- **Legal-action mask:** `getValidMoves(state)` — `src/engine/StateManager.js:254`.
  Enumerates exactly the legal `(from, to)` attacks each step. Use it to mask.
- **Exact dice odds:** `WIN_TABLE[a][d] = P(a dice beat d dice)` —
  `src/ai/diceOdds.js`. Don't make the model learn dice math; feed win-prob as a
  feature.
- **Determinism:** Mulberry32 RNG state lives inside `GameState`, threaded
  through every `applyAction`; same seed → identical game. Pass an explicit
  `config.seed`, and the policy must avoid `Math.random`. (Historical note:
  `ai_default`, `ai_example`, `ai_adaptive` called `Math.random` and were
  non-reproducible opponents until issue #151 moved them to the seeded
  per-decision `game.random()` — every built-in is seed-pure now.)
- **Measured throughput:** ~150 games/s/core pure engine (~6.6 ms/game, ~12
  µs/`applyAction`), ~77 g/s with the Strategist heuristic, near-linear across
  cores (the Strategist run hit ~266 g/s on 4 procs ≈ 3.4× its 77 g/s single
  core). 1M engine-bound games ≈ 14–28 min on 8 cores. **Inference, not the
  engine, will be the bottleneck.**
- **Training-mode perf (landed in Phase 1):** the O(n²) `history` append is skipped
  under `config.recordHistory:false` (the `appendHistory` guard in `StateManager.js`) —
  though [D-12] found that append was never the real throughput bottleneck. The actual
  lever, the per-move `cloneAreas`/`recalcPlayerStats` cost, was trimmed in Phase-1 task 3
  (≈1.9× engine-only; see `RESULTS.md`).
- **A turn is a _sequence_ of single attacks** ended by STOP (returning `null`) —
  model an explicit STOP action, not one batched move per turn.
- **Reward:** terminal win/placement; free dense shaping available =
  Δlargest-connected-group income, territory/dice deltas, eliminations (the same
  quantities `ai_strategist` optimizes).
- **Board:** up to 31 real territories (index 0 unused), MAX_DICE 8, default 7
  players, maps vary in size — pad/mask, don't assume a fixed node count.
